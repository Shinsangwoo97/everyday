var input = require("fs").readFileSync("../example.txt").toString().trim();

console.log(`${input}??!`);

const fs = require("fs");
const input = fs.readFileSync("/dev/stdin").toString().trim();

console.log(`${input}??!`);

// input ex)joonas, baekjoon
